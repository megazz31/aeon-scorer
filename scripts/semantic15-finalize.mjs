import fs from 'node:fs'

const read=p=>fs.readFileSync(p,'utf8')
const write=(p,s)=>fs.writeFileSync(p,s)
const patch=(p,fn)=>{const before=read(p),after=fn(before);if(after===before){console.log('unchanged',p);return false}write(p,after);console.log('patched',p);return true}
const replaceOnce=(s,from,to,label)=>{if(s.includes(to))return s;if(!s.includes(from))throw new Error(`semantic15 finalizer missing ${label}`);return s.replace(from,to)}
const replaceLine=(s,prefix,to,label)=>{if(s.includes(to))return s;const lines=s.split('\n'),i=lines.findIndex(x=>x.startsWith(prefix));if(i<0)throw new Error(`semantic15 finalizer missing line ${label}`);lines[i]=to;return lines.join('\n')}
const insertBefore=(s,marker,text,label)=>{if(s.includes(text.trim()))return s;if(!s.includes(marker))throw new Error(`semantic15 finalizer missing marker ${label}`);return s.replace(marker,`${text}${marker}`)}

patch('src/engine/cardFeatures.js',src=>{
  src=insertBefore(src,'function isRecursion(o)',`function isGraveyardSpellReplay(o){const s=String(o||'').toLowerCase();return /(?:instant or sorcery|instant and\\/or sorcery)[\\s\\S]{0,180}from your graveyard[\\s\\S]{0,260}copy (?:it|them|the card|those cards|each|that card)[\\s\\S]{0,180}(?:cast|without paying)/.test(s)}\n`,'graveyard replay helper')
  src=replaceLine(src,'function isRecursion(o)',`function isRecursion(o){const s=o.toLowerCase();if(/\\b(?<!-)(?:flashback|unearth|escape|disturb|retrace|jump-start|aftermath|dredge|encore|embalm|eternalize)\\b/.test(s))return true;return /return [^.]* from (?:your|a) graveyard/.test(s)||/cast [^.]* from your graveyard/.test(s)||/play [^.]* from your graveyard/.test(s)||/search your library and\\/or your graveyard for [^.]*put [^.]*onto the battlefield/.test(s)||isGraveyardSpellReplay(s)}`,'recursion')
  src=replaceLine(src,'function isGraveSetup(o)',`function isGraveSetup(o){const s=withoutReminderText(o).toLowerCase();if(/put all cards? exiled this way that (?:weren't|were not) cast into your graveyard/.test(s))return true;return clauses(s).some(c=>{if(/(?:target|each|an?) opponents? [^.]{0,80}discards?/.test(c)&&!/\\byou (?:may )?discard\\b/.test(c)&&!/each player discards?/.test(c))return false;return /\\bmill (?:\\d+|x|one|two|three|four|five)\\b|\\bsurveil\\b|\\byou (?:may )?discard (?:a|one|two|three|x|any number of) cards?\\b|\\bdiscard (?:a|one|two|three|x|any number of) cards?\\b|\\beach player discards?\\b|\\byou and [^.]{0,80} each discard\\b|put the top [^.]*cards? of (?:your|a) library into (?:your|its) graveyard/.test(c)})}`,'grave setup')
  src=replaceLine(src,'function exileCast(o)',`function exileCast(o){const s=withoutReminderText(o).toLowerCase();if(isGraveyardSpellReplay(s))return false;return /\\b(?:cast|play) [^.]{0,140} from exile\\b/.test(s)||/\\bexile .{0,280}\\b(?:you may|you can) (?:play|cast)\\b/.test(s)||/\\bexile .{0,280}\\bcast (?:it|them|those cards|any number|that card)\\b/.test(s)||/\\bplay (?:a land|lands?|a card|cards?) or cast (?:a spell|spells?) from among cards? exiled\\b/.test(s)}`,'exile cast')
  src=replaceLine(src,'function protection(o)',`function protection(card,o){const s=withoutReminderText(o).toLowerCase();const grants=/(?:target|equipped|enchanted) [^.]{0,100}(?:gains?|has) [^.]{0,80}(?:hexproof|indestructible|protection from|ward)|(?:creatures?|permanents?|artifacts?|enchantments?) you control [^.]{0,80}(?:gain|have) [^.]{0,80}(?:hexproof|indestructible|protection from|ward)|other [^.]{0,80} you control [^.]{0,80}(?:have|gain) [^.]{0,80}(?:hexproof|indestructible|protection from|ward)/.test(s);const reactive=/counter target spell [^.]*targets?|target [^.]{0,80} phases? out|phase out target/.test(s);return grants||reactive||(isBlinkText(s)&&/you control/.test(s))}`,'protection')
  src=src.split('\n').filter(line=>!line.includes("if(/\\bwhenever (?:you|a player) sacrifices?\\b/.test(c)&&!/\\bwhenever (?:you|a player) sacrifices? (?:an? artifact|a land|an enchantment|a clue|a treasure|a food|a junk|a blood)\\b/.test(c))return true")).join('\n')
  src=replaceOnce(src,"  if(/enters(?: the battlefield)?|whenever [^.]* enters/i.test(semantic))add('etb');if(isBlinkText(semantic))add('blink');", "  if(/\\bwhen(?:ever)?\\b[^.\\n;]{0,220}\\benters(?: the battlefield)?\\b/i.test(semantic))add('etb');if(isBlinkText(semantic))add('blink');",'trigger-only etb tag')
  src=replaceOnce(src,"  if(protection(semantic))add('protection');if(isRecursion(semantic))add('recursion');if(isGraveSetup(semantic))add('graveyard-setup')", "  if(protection(card,semantic))add('protection');else if(/\\b(?:hexproof|shroud|indestructible|protection from|ward)\\b/i.test(semantic))add('self-protection');if(isRecursion(semantic))add('recursion');if(isGraveSetup(semantic))add('graveyard-setup')",'protection call')
  return src
})

patch('src/engine/packageGraph.js',src=>{
  src=insertBefore(src,'const MOTIFS=[',`import { topLibraryCheatProfile, isTopLibraryCheatTarget } from './commanderMechanics.js'\n\n`,'commander mechanic import')
  src=insertBefore(src,'function trueEtbPayoff(c){',`function harmfulLeaveDownside(c){const o=semanticText(c);return /when(?:ever)? [^.]{0,100} leaves the battlefield[^.]{0,220}(?:you discard|discard \\w+ cards?|you lose \\d+ life|sacrifice \\w+ creatures?|sacrifice \\d+ creatures?)/.test(o)}\n`,'harmful LTB')
  src=replaceOnce(src,"function trueEtbPayoff(c){\n  if(c.isLand||/\\bland\\b/i.test(c.type||''))return false", "function trueEtbPayoff(c){\n  if(c.isLand||/\\bland\\b/i.test(c.type||''))return false\n  if(harmfulLeaveDownside(c))return false",'LTB exclusion')
  src=replaceOnce(src,"  if(scope){semantic.add('target-cost-reduction');custom.push(...cards.filter(c=>spellTargetsScope(c,scope)))}\n  if(isEnchantmentCastPayoff(commander))", "  if(scope){semantic.add('target-cost-reduction');custom.push(...cards.filter(c=>spellTargetsScope(c,scope)))}\n  const cheatProfile=topLibraryCheatProfile(commander)\n  if(cheatProfile){semantic.add('top-library-cheat');custom.push(...cards.filter(c=>isTopLibraryCheatTarget(c,cheatProfile)))}\n  if(isEnchantmentCastPayoff(commander))",'top-library commander synergy')
  return src
})

patch('src/engine/sequenceSimulator.js',src=>{
  src=replaceOnce(src,"import { isImmediateLandRamp } from './packageGraph.js'", "import { isImmediateLandRamp } from './packageGraph.js'\nimport { targetGenericReduction, targetCostReductionStats, topLibraryCheatProfile, isTopLibraryCheatTarget } from './commanderMechanics.js'",'sequence mechanic import')
  src=replaceLine(src,'function paymentOptions(card,tax=0)',`function paymentOptions(card,tax=0,genericReduction=0){const req=card.manaReq||{generic:Math.max(0,Number(card.cmc||0)),colored:[]},colored=(req.colored||[]).map(opts=>opts.length?opts:['C']),represented=Number(req.generic||0)+colored.length,baseGeneric=Math.max(0,Number(req.generic||0)+tax),reduction=Math.min(baseGeneric,Math.max(0,Number(genericReduction||0))),baseTotal=Math.max(Number(card.cmc||represented),represented)+tax,total=Math.max(colored.length,baseTotal-reduction),generic=Math.max(0,baseGeneric-reduction,total-colored.length);return {colored,generic,total}}`,'payment options')
  src=replaceLine(src,'function concretePaymentIndices(card,sources,tax=0)',`function concretePaymentIndices(card,sources,tax=0,genericReduction=0){const req=paymentOptions(card,tax,genericReduction);if(sources.length<req.total)return null;const pips=[...req.colored].sort((a,b)=>a.length-b.length),used=new Set();function place(i){if(i>=pips.length){const remaining=[];for(let s=0;s<sources.length;s++)if(!used.has(s))remaining.push(s);if(remaining.length<req.generic)return null;return new Set([...used,...remaining.slice(0,req.generic)])}for(let s=0;s<sources.length;s++){if(used.has(s)||!pips[i].some(c=>sources[s].options.includes(c)))continue;used.add(s);const result=place(i+1);if(result)return result;used.delete(s)}return null}return place(0)}`,'payment indices')
  src=replaceLine(src,'function paymentResult(card,sources,tax=0)',`function paymentResult(card,sources,tax=0,genericReduction=0){for(const pool of expandManaPools(sources)){const used=concretePaymentIndices(card,pool,tax,genericReduction);if(used)return {pool,used}}return null}`,'payment result')
  src=replaceLine(src,'export function canPay(card,sources,tax=0)',`export function canPay(card,sources,tax=0,genericReduction=0){return paymentResult(card,sources,tax,genericReduction)!==null}`,'canPay')
  src=replaceLine(src,'function payAndRemain(card,sources,tax=0)',`function payAndRemain(card,sources,tax=0,genericReduction=0){const result=paymentResult(card,sources,tax,genericReduction);return result?result.pool.filter((_,i)=>!result.used.has(i)):null}`,'payAndRemain')
  src=insertBefore(src,'function keepOpeningHand(hand)',`function canPayWithCommanderMechanic(card,sources,commander,alreadyOnline=false,postCommanderSources=null){if(canPay(card,sources))return true;const reduction=targetGenericReduction(card,commander);if(!reduction)return false;if(alreadyOnline&&canPay(card,sources,0,reduction))return true;return !!postCommanderSources&&canPay(card,postCommanderSources,0,reduction)}\nfunction unlockedByCommanderMechanic(card,sources,commander,alreadyOnline=false,postCommanderSources=null){if(canPay(card,sources))return false;const reduction=targetGenericReduction(card,commander);if(!reduction)return false;return alreadyOnline?canPay(card,sources,0,reduction):!!postCommanderSources&&canPay(card,postCommanderSources,0,reduction)}\nfunction resolveTopLibraryCheat(lib,battlefield,profile){if(!profile?.look||!lib.length)return {triggered:false,hit:false,card:null,compressed:0};const looked=lib.splice(0,Math.min(profile.look,lib.length)),eligible=looked.filter(c=>isTopLibraryCheatTarget(c,profile)).sort((a,b)=>(Number(b.cmc||0)+Number(b.development||0)+Number(b.explosiveness||0))-(Number(a.cmc||0)+Number(a.development||0)+Number(a.explosiveness||0))||String(a.name||'').localeCompare(String(b.name||''))),chosen=eligible[0]||null;if(chosen)battlefield.push(chosen);for(const c of looked)if(c!==chosen)lib.push(c);return {triggered:true,hit:!!chosen,card:chosen,compressed:chosen?Number(chosen.cmc||0):0}}\n`,'sequence mechanic helpers')
  src=replaceOnce(src,"  const libBase=cards.filter(c=>!commander||c.name.toLowerCase()!==commander.name.toLowerCase()||c.__keepIn99),samples=[],cmdTurns=[],engineTurns=[],recoverySamples=[]\n  const turnStats=Array.from({length:maxTurn},()=>({cmd:0,engine:0,interaction:0,resource:0,burst:0,total:0}))", "  const libBase=cards.filter(c=>!commander||c.name.toLowerCase()!==commander.name.toLowerCase()||c.__keepIn99),samples=[],cmdTurns=[],engineTurns=[],recoverySamples=[]\n  const targetStats=targetCostReductionStats(cards,commander),cheatProfile=topLibraryCheatProfile(commander);let cheatTriggers=0,cheatHits=0,cheatCompressed=0,targetUnlockEvents=0\n  const turnStats=Array.from({length:maxTurn},()=>({cmd:0,engine:0,interaction:0,resource:0,burst:0,commanderCheat:0,targetReduction:0,total:0}))",'single mechanic stats')
  const oldBlock=`      const generalSources=potentialSources(turnSources,hand,used,false,battlefield),commanderSources=potentialSources(turnSources,hand,used,true,battlefield)\n      if(commander&&cmdTurn==null&&canPay(commander,commanderSources)){cmdTurn=turn;battlefield.push(commander)}\n      const engine=operationalPackage(hand,priorHand,battlefield,used,packages,generalSources,priorSources);if(engine.ok&&engineTurn==null)engineTurn=turn;if(turn===4&&engine.ok)disruptedPackageId=engine.packageId\n      const interaction=castableCards(hand,used,generalSources,c=>c.interaction>0).length>0,resource=castableCards(hand,used,generalSources,c=>c.tags.includes('draw')||c.tags.includes('recursion')).length>0\n      const manaBurst=maxManaCount(generalSources)>=maxManaCount(turnSources)+2,highImpact=castableCards(hand,used,generalSources,c=>c.tags.includes('extra-turn')||c.tags.includes('win')||c.tags.includes('cheat')).length>0,combo=comboAccessible(hand,priorHand,battlefield,used,combos,generalSources,priorSources),burst=manaBurst||highImpact||combo`
  const newBlock=`      const generalSources=potentialSources(turnSources,hand,used,false,battlefield),commanderSources=potentialSources(turnSources,hand,used,true,battlefield)\n      const wasCommanderOnline=!!commander&&cmdTurn!=null&&cmdTurn<turn;let commanderCastThisTurn=false,postCommanderSources=null\n      if(commander&&cmdTurn==null&&canPay(commander,commanderSources)){postCommanderSources=payAndRemain(commander,commanderSources);cmdTurn=turn;commanderCastThisTurn=true;battlefield.push(commander)}\n      let cheatCard=null,cheatTriggeredThisTurn=false\n      if(commander&&cheatProfile){const triggers=(commanderCastThisTurn&&cheatProfile.onEnter?1:0)+((wasCommanderOnline||commanderCastThisTurn&&cheatProfile.haste)&&cheatProfile.onAttack?1:0);for(let n=0;n<triggers;n++){const hit=resolveTopLibraryCheat(lib,battlefield,cheatProfile);if(hit.triggered){cheatTriggers++;cheatTriggeredThisTurn=true}if(hit.hit){cheatHits++;cheatCompressed+=hit.compressed;cheatCard=hit.card}}}\n      const mechanicPay=c=>canPayWithCommanderMechanic(c,generalSources,commander,wasCommanderOnline,commanderCastThisTurn?postCommanderSources:null),targetUnlock=!!targetStats&&hand.some(c=>!used.has(c)&&unlockedByCommanderMechanic(c,generalSources,commander,wasCommanderOnline,commanderCastThisTurn?postCommanderSources:null));if(targetUnlock)targetUnlockEvents++\n      const engine=operationalPackage(hand,priorHand,battlefield,used,packages,generalSources,priorSources);if(engine.ok&&engineTurn==null)engineTurn=turn;if(turn===4&&engine.ok)disruptedPackageId=engine.packageId\n      const interaction=!!(cheatCard&&cheatCard.interaction>0)||hand.some(c=>!used.has(c)&&c.interaction>0&&mechanicPay(c)),resource=!!(cheatCard&&(cheatCard.tags.includes('draw')||cheatCard.tags.includes('recursion')))||hand.some(c=>!used.has(c)&&(c.tags.includes('draw')||c.tags.includes('recursion'))&&mechanicPay(c))\n      const manaBurst=maxManaCount(generalSources)>=maxManaCount(turnSources)+2,highImpact=!!(cheatCard&&(Number(cheatCard.cmc||0)>=5||cheatCard.tags.includes('win')||cheatCard.tags.includes('cheat')))||hand.some(c=>!used.has(c)&&(c.tags.includes('extra-turn')||c.tags.includes('win')||c.tags.includes('cheat'))&&mechanicPay(c)),combo=comboAccessible(hand,priorHand,battlefield,used,combos,generalSources,priorSources),burst=manaBurst||highImpact||combo`
  src=replaceOnce(src,oldBlock,newBlock,'single simulation mechanic block')
  src=replaceOnce(src,"      peak=Math.max(peak,state);sum+=state;const ts=turnStats[turn-1];ts.total++;ts.cmd+=cmdOnline?1:0;ts.engine+=engine.ok?1:0;ts.interaction+=interaction?1:0;ts.resource+=resource?1:0;ts.burst+=burst?1:0;priorSources=[...turnSources]", "      peak=Math.max(peak,state);sum+=state;const ts=turnStats[turn-1];ts.total++;ts.cmd+=cmdOnline?1:0;ts.engine+=engine.ok?1:0;ts.interaction+=interaction?1:0;ts.resource+=resource?1:0;ts.burst+=burst?1:0;ts.commanderCheat+=cheatTriggeredThisTurn?1:0;ts.targetReduction+=targetUnlock?1:0;priorSources=[...turnSources]",'single turn stats')
  src=replaceOnce(src,"  const turnProfile=turnStats.map((s,i)=>({turn:i+1,commander:Math.round(s.cmd/Math.max(1,s.total)*100),engine:Math.round(s.engine/Math.max(1,s.total)*100),interaction:Math.round(s.interaction/Math.max(1,s.total)*100),resource:Math.round(s.resource/Math.max(1,s.total)*100),burst:Math.round(s.burst/Math.max(1,s.total)*100)}))\n  return {iterations,floor:Math.round(floor),median:Math.round(median),high:Math.round(high),ceiling:Math.round(high),peak:Math.round(peak),iqr:Math.round(iqr),consistency:Math.round(consistency),commanderMedianTurn:cmdTurns.length?Math.round(percentile(cmdTurns,.5)*10)/10:null,engineMedianTurn:engineTurns.length?Math.round(percentile(engineTurns,.5)*10)/10:null,recoveryAfterDisruption:Math.round(recoverySamples.reduce((s,x)=>s+x,0)/Math.max(1,recoverySamples.length)*100),turnProfile}", "  const turnProfile=turnStats.map((s,i)=>({turn:i+1,commander:Math.round(s.cmd/Math.max(1,s.total)*100),engine:Math.round(s.engine/Math.max(1,s.total)*100),interaction:Math.round(s.interaction/Math.max(1,s.total)*100),resource:Math.round(s.resource/Math.max(1,s.total)*100),burst:Math.round(s.burst/Math.max(1,s.total)*100),commanderCheat:Math.round(s.commanderCheat/Math.max(1,s.total)*100),targetReduction:Math.round(s.targetReduction/Math.max(1,s.total)*100)}))\n  const commanderMechanics={targetCostReduction:targetStats?{...targetStats,unlockRate:Math.round(targetUnlockEvents/Math.max(1,iterations*maxTurn)*1000)/10}:null,topLibraryCheat:cheatProfile?{profile:cheatProfile,triggers:cheatTriggers,hits:cheatHits,hitRate:Math.round(cheatHits/Math.max(1,cheatTriggers)*1000)/10,averageManaCompressed:Math.round(cheatCompressed/Math.max(1,cheatHits)*10)/10}:null}\n  return {iterations,floor:Math.round(floor),median:Math.round(median),high:Math.round(high),ceiling:Math.round(high),peak:Math.round(peak),iqr:Math.round(iqr),consistency:Math.round(consistency),commanderMedianTurn:cmdTurns.length?Math.round(percentile(cmdTurns,.5)*10)/10:null,engineMedianTurn:engineTurns.length?Math.round(percentile(engineTurns,.5)*10)/10:null,recoveryAfterDisruption:Math.round(recoverySamples.reduce((s,x)=>s+x,0)/Math.max(1,recoverySamples.length)*100),turnProfile,commanderMechanics}",'single return mechanics')
  return src
})

patch('src/engine/sequenceSimulatorMulti.js',src=>{
  src=replaceOnce(src,"import { commanderPriorityProfile } from './commanderPair.js'", "import { commanderPriorityProfile } from './commanderPair.js'\nimport { combinedTargetGenericReduction, targetCostReductionStats, topLibraryCheatProfile, isTopLibraryCheatTarget } from './commanderMechanics.js'",'multi mechanic import')
  src=insertBefore(src,'function keepOpeningHand(hand)',`function mechanicPayMulti(card,sources,onlineCommanders=[],sameTurnCommanders=[],postCommanderSources=null){if(canPay(card,sources))return true;const onlineReduction=combinedTargetGenericReduction(card,onlineCommanders);if(onlineReduction&&canPay(card,sources,0,onlineReduction))return true;const all=[...onlineCommanders,...sameTurnCommanders],allReduction=combinedTargetGenericReduction(card,all);return !!postCommanderSources&&allReduction>0&&canPay(card,postCommanderSources,0,allReduction)}\nfunction mechanicUnlockMulti(card,sources,onlineCommanders=[],sameTurnCommanders=[],postCommanderSources=null){if(canPay(card,sources))return false;return mechanicPayMulti(card,sources,onlineCommanders,sameTurnCommanders,postCommanderSources)}\nfunction resolveTopLibraryCheatMulti(lib,battlefield,profile){if(!profile?.look||!lib.length)return {triggered:false,hit:false,card:null,compressed:0};const looked=lib.splice(0,Math.min(profile.look,lib.length)),eligible=looked.filter(c=>isTopLibraryCheatTarget(c,profile)).sort((a,b)=>Number(b.cmc||0)-Number(a.cmc||0)||String(a.name||'').localeCompare(String(b.name||''))),chosen=eligible[0]||null;if(chosen)battlefield.push(chosen);for(const c of looked)if(c!==chosen)lib.push(c);return {triggered:true,hit:!!chosen,card:chosen,compressed:chosen?Number(chosen.cmc||0):0}}\n`,'multi mechanic helpers')
  src=replaceOnce(src,"  const libBase=cards.filter(c=>!cmdNames.has(c.name.toLowerCase())||c.__keepIn99),samples=[],engineTurns=[],recoverySamples=[],firstCmdTurns=[],perCmdTurns=cmd.map(()=>[])\n  const turnStats=Array.from({length:maxTurn},()=>({cmd:0,both:0,per:cmd.map(()=>0),engine:0,interaction:0,resource:0,burst:0,total:0}))", "  const libBase=cards.filter(c=>!cmdNames.has(c.name.toLowerCase())||c.__keepIn99),samples=[],engineTurns=[],recoverySamples=[],firstCmdTurns=[],perCmdTurns=cmd.map(()=>[])\n  const targetProfiles=cmd.map((c,i)=>({i,name:c.name,stats:targetCostReductionStats(cards,c)})).filter(x=>x.stats),cheatProfiles=cmd.map((c,i)=>({i,name:c.name,profile:topLibraryCheatProfile(c)})).filter(x=>x.profile);let targetUnlockEvents=0,cheatTriggers=0,cheatHits=0,cheatCompressed=0\n  const turnStats=Array.from({length:maxTurn},()=>({cmd:0,both:0,per:cmd.map(()=>0),engine:0,interaction:0,resource:0,burst:0,commanderCheat:0,targetReduction:0,total:0}))",'multi stats')
  const old=`      const generalSources=potentialSources(turnSources,hand,used,false,battlefield),commanderSources=potentialSources(turnSources,hand,used,true,battlefield)\n      const castPlan=planCommanderCasts(cmd,cmdTurns,castCounts,commanderSources)\n      for(const i of castPlan.indices){cmdTurns[i]=turn;castCounts[i]++;battlefield.push(cmd[i])}\n      const engine=operationalPackage(hand,priorHand,battlefield,used,packages,generalSources,priorSources);if(engine.ok&&engineTurn==null)engineTurn=turn;if(turn===4&&engine.ok)disruptedPackageId=engine.packageId\n      const interaction=castableCards(hand,used,generalSources,c=>c.interaction>0).length>0,resource=castableCards(hand,used,generalSources,c=>c.tags.includes('draw')||c.tags.includes('recursion')).length>0\n      const manaBurst=maxManaCount(generalSources)>=maxManaCount(turnSources)+2,highImpact=castableCards(hand,used,generalSources,c=>c.tags.includes('extra-turn')||c.tags.includes('win')||c.tags.includes('cheat')).length>0,combo=comboAccessible(hand,priorHand,battlefield,used,combos,generalSources,priorSources),burst=manaBurst||highImpact||combo`
  const neu=`      const generalSources=potentialSources(turnSources,hand,used,false,battlefield),commanderSources=potentialSources(turnSources,hand,used,true,battlefield),onlineBefore=cmd.filter((_,i)=>cmdTurns[i]!=null&&cmdTurns[i]<turn)\n      const castPlan=planCommanderCasts(cmd,cmdTurns,castCounts,commanderSources),sameTurn=castPlan.indices.map(i=>cmd[i])\n      for(const i of castPlan.indices){cmdTurns[i]=turn;castCounts[i]++;battlefield.push(cmd[i])}\n      let cheatCard=null,cheatTriggeredThisTurn=false\n      for(const entry of cheatProfiles){const castNow=castPlan.indices.includes(entry.i),wasOnline=cmdTurns[entry.i]!=null&&cmdTurns[entry.i]<turn,triggers=(castNow&&entry.profile.onEnter?1:0)+((wasOnline||castNow&&entry.profile.haste)&&entry.profile.onAttack?1:0);for(let n=0;n<triggers;n++){const hit=resolveTopLibraryCheatMulti(lib,battlefield,entry.profile);if(hit.triggered){cheatTriggers++;cheatTriggeredThisTurn=true}if(hit.hit){cheatHits++;cheatCompressed+=hit.compressed;cheatCard=hit.card}}}\n      const mechanicPay=c=>mechanicPayMulti(c,generalSources,onlineBefore,sameTurn,castPlan.remaining),targetUnlock=targetProfiles.length>0&&hand.some(c=>!used.has(c)&&mechanicUnlockMulti(c,generalSources,onlineBefore,sameTurn,castPlan.remaining));if(targetUnlock)targetUnlockEvents++\n      const engine=operationalPackage(hand,priorHand,battlefield,used,packages,generalSources,priorSources);if(engine.ok&&engineTurn==null)engineTurn=turn;if(turn===4&&engine.ok)disruptedPackageId=engine.packageId\n      const interaction=!!(cheatCard&&cheatCard.interaction>0)||hand.some(c=>!used.has(c)&&c.interaction>0&&mechanicPay(c)),resource=!!(cheatCard&&(cheatCard.tags.includes('draw')||cheatCard.tags.includes('recursion')))||hand.some(c=>!used.has(c)&&(c.tags.includes('draw')||c.tags.includes('recursion'))&&mechanicPay(c))\n      const manaBurst=maxManaCount(generalSources)>=maxManaCount(turnSources)+2,highImpact=!!(cheatCard&&(Number(cheatCard.cmc||0)>=5||cheatCard.tags.includes('win')||cheatCard.tags.includes('cheat')))||hand.some(c=>!used.has(c)&&(c.tags.includes('extra-turn')||c.tags.includes('win')||c.tags.includes('cheat'))&&mechanicPay(c)),combo=comboAccessible(hand,priorHand,battlefield,used,combos,generalSources,priorSources),burst=manaBurst||highImpact||combo`
  src=replaceOnce(src,old,neu,'multi mechanic block')
  src=replaceOnce(src,"      peak=Math.max(peak,state);sum+=state;const ts=turnStats[turn-1];ts.total++;ts.cmd+=cmdOnline?1:0;ts.both+=bothOnline?1:0;online.forEach((x,i)=>ts.per[i]+=x?1:0);ts.engine+=engine.ok?1:0;ts.interaction+=interaction?1:0;ts.resource+=resource?1:0;ts.burst+=burst?1:0;priorSources=[...turnSources]", "      peak=Math.max(peak,state);sum+=state;const ts=turnStats[turn-1];ts.total++;ts.cmd+=cmdOnline?1:0;ts.both+=bothOnline?1:0;online.forEach((x,i)=>ts.per[i]+=x?1:0);ts.engine+=engine.ok?1:0;ts.interaction+=interaction?1:0;ts.resource+=resource?1:0;ts.burst+=burst?1:0;ts.commanderCheat+=cheatTriggeredThisTurn?1:0;ts.targetReduction+=targetUnlock?1:0;priorSources=[...turnSources]",'multi turn stats')
  src=replaceOnce(src,"  const turnProfile=turnStats.map((s,i)=>({turn:i+1,commander:Math.round(s.cmd/Math.max(1,s.total)*100),bothCommanders:Math.round(s.both/Math.max(1,s.total)*100),commanders:Object.fromEntries(cmd.map((c,j)=>[c.name,Math.round(s.per[j]/Math.max(1,s.total)*100)])),engine:Math.round(s.engine/Math.max(1,s.total)*100),interaction:Math.round(s.interaction/Math.max(1,s.total)*100),resource:Math.round(s.resource/Math.max(1,s.total)*100),burst:Math.round(s.burst/Math.max(1,s.total)*100)}))", "  const turnProfile=turnStats.map((s,i)=>({turn:i+1,commander:Math.round(s.cmd/Math.max(1,s.total)*100),bothCommanders:Math.round(s.both/Math.max(1,s.total)*100),commanders:Object.fromEntries(cmd.map((c,j)=>[c.name,Math.round(s.per[j]/Math.max(1,s.total)*100)])),engine:Math.round(s.engine/Math.max(1,s.total)*100),interaction:Math.round(s.interaction/Math.max(1,s.total)*100),resource:Math.round(s.resource/Math.max(1,s.total)*100),burst:Math.round(s.burst/Math.max(1,s.total)*100),commanderCheat:Math.round(s.commanderCheat/Math.max(1,s.total)*100),targetReduction:Math.round(s.targetReduction/Math.max(1,s.total)*100)}))",'multi profile')
  src=replaceOnce(src,"  return {iterations,floor:Math.round(floor),median:Math.round(median),high:Math.round(high),ceiling:Math.round(high),peak:Math.round(peak),iqr:Math.round(iqr),consistency:Math.round(consistency),commanderMedianTurn:firstCmdTurns.length?Math.round(percentile(firstCmdTurns,.5)*10)/10:null,commanderMedianTurns,engineMedianTurn:engineTurns.length?Math.round(percentile(engineTurns,.5)*10)/10:null,recoveryAfterDisruption:Math.round(recoverySamples.reduce((s,x)=>s+x,0)/Math.max(1,recoverySamples.length)*100),turnProfile,commandZone:{count:cmd.length,names:cmd.map(c=>c.name),separateTax:true}}", "  const commanderMechanics={targetCostReduction:targetProfiles.length?{commanders:targetProfiles.map(x=>({name:x.name,...x.stats})),unlockRate:Math.round(targetUnlockEvents/Math.max(1,iterations*maxTurn)*1000)/10}:null,topLibraryCheat:cheatProfiles.length?{commanders:cheatProfiles.map(x=>({name:x.name,profile:x.profile})),triggers:cheatTriggers,hits:cheatHits,hitRate:Math.round(cheatHits/Math.max(1,cheatTriggers)*1000)/10,averageManaCompressed:Math.round(cheatCompressed/Math.max(1,cheatHits)*10)/10}:null}\n  return {iterations,floor:Math.round(floor),median:Math.round(median),high:Math.round(high),ceiling:Math.round(high),peak:Math.round(peak),iqr:Math.round(iqr),consistency:Math.round(consistency),commanderMedianTurn:firstCmdTurns.length?Math.round(percentile(firstCmdTurns,.5)*10)/10:null,commanderMedianTurns,engineMedianTurn:engineTurns.length?Math.round(percentile(engineTurns,.5)*10)/10:null,recoveryAfterDisruption:Math.round(recoverySamples.reduce((s,x)=>s+x,0)/Math.max(1,recoverySamples.length)*100),turnProfile,commanderMechanics,commandZone:{count:cmd.length,names:cmd.map(c=>c.name),separateTax:true}}",'multi return')
  return src
})

patch('src/engine/powerModel.js',src=>{
  src=replaceOnce(src,"  const dimensions={speed:Math.round(speed),consistency:Math.round(consistency),explosiveness:Math.round(explosiveness),synergy:Math.round(synergy),interaction:Math.round(interaction),resilience:Math.round(resilience)},warnings=[],targetDiscountNotSimulated=commanders.some(hasTargetCommanderDiscount),topLibraryCheatNotSimulated=commanders.some(hasTopLibraryCreatureCheat)", "  const dimensions={speed:Math.round(speed),consistency:Math.round(consistency),explosiveness:Math.round(explosiveness),synergy:Math.round(synergy),interaction:Math.round(interaction),resilience:Math.round(resilience)},warnings=[],targetReduction=sim?.commanderMechanics?.targetCostReduction||null,topLibraryCheat=sim?.commanderMechanics?.topLibraryCheat||null,targetReductionXConservative=!!targetReduction&&cards.some(c=>/\\{X\\}/i.test(String(c.manaCost||''))&&/\\btarget\\b/i.test(String(c.oracle||'')))",'power mechanics vars')
  src=src.split('\n').filter(line=>!line.includes('if(targetDiscountNotSimulated)warnings.push')&&!line.includes('if(topLibraryCheatNotSimulated)warnings.push')).join('\n')
  src=replaceOnce(src,"  const limitations=[]\n  if(targetDiscountNotSimulated)limitations.push('commander-target-cost-reduction-not-simulated')\n  if(topLibraryCheatNotSimulated)limitations.push('commander-top-library-cheat-not-simulated')", "  const limitations=[]\n  if(targetReductionXConservative)limitations.push('target-cost-reduction-x-value-conservative')\n  if(targetReductionXConservative)warnings.push('Réduction de coût liée aux cibles simulée sur le mana générique connu ; les valeurs X choisies restent volontairement conservatrices.')",'power limitations')
  src=replaceOnce(src,"methodology:{iterations,firstAccessIterations,model:'sequence-access-v3.2-semantic',maxTurn:7,commandZoneCount:commanders.length,separateCommanderTax:multi,curveMeaning:'Chaque colonne mesure un accès indépendant ; elles ne représentent pas une même ligne de jeu simultanée.',limitations}", "methodology:{iterations,firstAccessIterations,model:'sequence-access-v3.2-semantic',maxTurn:7,commandZoneCount:commanders.length,separateCommanderTax:multi,curveMeaning:'Chaque colonne mesure un accès indépendant ; elles ne représentent pas une même ligne de jeu simultanée.',commanderMechanics:sim.commanderMechanics||null,limitations}",'methodology mechanics')
  return src
})

patch('scripts/deck-prefill-contract-test.mjs',src=>{
  src=replaceOnce(src,"assert.equal(shareRow.median, 55)\n\nconst norm = normalizedShare(shareRow)\nassert.equal(norm.deckName, 'Blood Rites')\nassert.equal(norm.median, 55)\n\nconst roadmapRes = roadmapResultFromShare(shareRow)\nassert.equal(roadmapRes.profile.median, 55)", "const bloodMedian = Number(preconJson.analysis?.median ?? preconJson.result?.profile?.median)\nassert.ok(Number.isFinite(bloodMedian), 'Blood Rites precon must expose its generated median')\nassert.equal(shareRow.median, bloodMedian)\n\nconst norm = normalizedShare(shareRow)\nassert.equal(norm.deckName, 'Blood Rites')\nassert.equal(norm.median, bloodMedian)\n\nconst roadmapRes = roadmapResultFromShare(shareRow)\nassert.equal(roadmapRes.profile.median, bloodMedian)",'dynamic precon median')
  return src
})

patch('package.json',src=>replaceOnce(src,'node scripts/hinata-model-limit-test.mjs && node scripts/semantic15-audit-regression-test.mjs','node scripts/hinata-model-limit-test.mjs && node scripts/semantic15-audit-regression-test.mjs && node scripts/semantic15-sequence-regression-test.mjs','semantic15 sequence test script'))

console.log('SEMANTIC-15 FINALIZER APPLIED')
