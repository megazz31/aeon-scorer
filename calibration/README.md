# Calibration workspace

This directory is populated by `scripts/benchmark.mjs` during the calibration workflow. Generated reports are uploaded as workflow artifacts and are not intended to be committed during iterative tuning.
