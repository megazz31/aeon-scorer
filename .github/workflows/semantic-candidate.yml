# Generic gate for immutable semantic release candidates.
name: Semantic candidate validation

on:
  push:
    branches: ['semantic/**']
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: semantic-candidate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install
        run: npm ci
      - name: Fast semantic + product preflight
        run: |
          npm run test:semantic
          npm run test:product
      - name: Generate candidate precon snapshots for semantic gate
        env:
          AEON_PRECON_ITERATIONS: '3200'
        run: |
          npm run precons:generate
          node scripts/update-public-precon-doc.mjs
      - name: Smoke
        run: npm run smoke
      - name: Semantic truth + sentinel precons
        run: npm run test:semantic:fast
      - name: Metamorphic contracts
        run: npm run test:metamorphic
      - name: Product contracts
        run: npm run test:product
      - name: Public precon contract
        run: npm run test:public-precons
      - name: Adversarial audit
        run: npm run audit
      - name: Build
        run: npm run build

  calibration:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    needs: quality
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install
        run: npm ci
      - name: Generate semantic candidate precon delta from main baseline
        env:
          AEON_PRECON_ITERATIONS: '3200'
        run: |
          set -euo pipefail
          AEON_SEMANTIC_BASELINE_REF="$(git merge-base HEAD origin/main)"
          npm run precons:generate
          node scripts/semantic-precon-delta.mjs "$AEON_SEMANTIC_BASELINE_REF"
          node scripts/semantic-precon-outlier-review.mjs
      - name: Pin mid-power validation cohort
        run: |
          mkdir -p calibration/cache
          cat > calibration/cache/arch_listing_MegazZ31.json <<'EOF'
          {"results":[{"id":25464836},{"id":25449616},{"id":18888572},{"id":24925767},{"id":25449328},{"id":23299336},{"id":23299341},{"id":23294619},{"id":25448552}]}
          EOF
      - name: Macro benchmark 1800
        env:
          AEON_BENCH_ITERATIONS: '1800'
          ARCHIDEKT_USERNAME: 'MegazZ31'
        run: npm run benchmark
      - name: Validate 1800
        run: |
          npm run validate:calibration
          node scripts/stamp-calibration-report.mjs
      - name: Save 1800
        run: cp calibration/latest.json calibration/run-1800.json
      - name: Macro benchmark 3200
        env:
          AEON_BENCH_ITERATIONS: '3200'
          ARCHIDEKT_USERNAME: 'MegazZ31'
        run: npm run benchmark
      - name: Validate 3200
        run: |
          npm run validate:calibration
          node scripts/stamp-calibration-report.mjs
      - name: Save 3200
        run: cp calibration/latest.json calibration/run-3200.json
      - name: Convergence
        run: npm run verify:sampling
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: semantic-candidate-validation
          path: |
            calibration/semantic-precon-delta.json
            calibration/semantic-precon-delta.md
            calibration/semantic-outlier-review.json
            calibration/semantic-outlier-review.md
            calibration/latest.json
            calibration/latest.md
            calibration/run-1800.json
            calibration/run-3200.json
            public/precons/catalog.json
            public/precons/*.json
          if-no-files-found: warn
